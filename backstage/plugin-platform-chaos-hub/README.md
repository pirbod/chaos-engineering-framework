# Platform Chaos Hub Backstage Plugin Skeleton

This is a lightweight plugin-style page rather than a published Backstage package. It demonstrates the intended developer portal experience:

- service maturity score
- approved chaos experiments
- ownership metadata
- runbook links
- observability links

Teams can copy `src/PlatformChaosHubPage.tsx` into an internal Backstage plugin and wire it to the backend API endpoints.
